node_modules/
.agents/loop-state/
.phpunit.result.cache
screenshots/
.aidevops.json
.beads
coverage.xml
coverage-html/
# Playwright
playwright-report/
test-results/
blob-report/
# Benchmark results (contain API-specific data, regenerated on demand)
tests/benchmark/results/
# Local AI tooling artifacts
.claude/
.agents/skills/
skills-lock.json
# Local wp-env port overrides
.wp-env.override.json

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Build artifacts — generated by CI (npm run build / composer install --no-dev)
*.zip
build/
vendor/
